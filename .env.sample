# Pinergy API client — copy to .env and fill for integration tests / CLI
# Get credentials from your Pinergy app account.

# Option A: email + password (client will login and get token)
PINERGY_EMAIL=me@mail.ie
PINERGY_PASSWORD=my_password

# Option B: use an existing auth token (e.g. from a previous login)
# PINERGY_AUTH_TOKEN=your_auth_token_here

# Optional: override API base URL (default: https://api.pinergy.ie/api)
# PINERGY_BASE_URL=https://api.pinergy.ie/api
